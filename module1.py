# shift elements in string or even list
def password_generator(txt):
  something = ""
  for i in range(0,len(txt)):
    password += username[i-index] # enter the index
  return something
print(password_generator("henry"))